# vim: set ft=zsh:
# Work-Specific Aliases
# Aliases and shortcuts specific to work environments

# Work environment shortcuts
alias work-vpn='echo "Connect to work VPN (customize this command)"'
alias work-ssh='echo "SSH to work servers (customize this command)"'

# Corporate tool shortcuts (customize based on your work environment)
# alias jira='open https://yourcompany.atlassian.net'
# alias confluence='open https://yourcompany.atlassian.net/wiki'
# alias slack='open -a Slack'
# alias teams='open -a "Microsoft Teams"'

# Work-specific git aliases
alias gwork='git config user.email "your.work.email@company.com"'
alias gpersonal='git config user.email "your.personal.email@domain.com"'

# Work project navigation (customize paths)
# alias proj-main='cd ~/work/main-project'
# alias proj-api='cd ~/work/api-project'
# alias proj-frontend='cd ~/work/frontend-project'

# Work-specific development shortcuts
# alias work-env='export ENVIRONMENT=development'
# alias work-staging='export ENVIRONMENT=staging'
# alias work-prod='export ENVIRONMENT=production'

# Corporate network utilities
# alias corp-ping='ping internal.company.com'
# alias corp-dns='nslookup internal.company.com'

# Work-specific Docker/Kubernetes shortcuts
# alias work-k8s='kubectl --context=work-cluster'
# alias work-docker='docker --context=work'

# Security and compliance shortcuts
# alias security-scan='echo "Run security scan (customize command)"'
# alias compliance-check='echo "Run compliance check (customize command)"'

# Productivity shortcuts for work
alias standup='echo "Daily standup notes (customize this)"'
alias eod='echo "End of day summary (customize this)"'

# Work-specific file operations
# alias work-backup='rsync -av ~/work/ /backup/work/'
# alias work-sync='echo "Sync work files (customize command)"'

# Work-specific aliases loaded - customize for your environment
